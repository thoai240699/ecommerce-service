server:
  port: 8080
  servlet:
    context-path: /ecommerce

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/ecommerce_service"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
